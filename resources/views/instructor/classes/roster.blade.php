@extends('layouts.instructor')
@section('title', 'Roster — '.$class->code)
@php($activeNav='classes')
@section('page-title-bar')<div class="page-title-bar"><h2 class="page-title"><i class="fa-solid fa-users"></i> Roster — {{ $class->code }}</h2><div class="page-actions"><a href="{{ route('instructor.classes.show',$class) }}" class="btn-modal-cancel">Back to Class</a></div></div>@endsection
@section('content')
<div class="dash-stats"><div class="dash-stat"><span class="dash-stat-label">Total</span><span class="dash-stat-value">{{ $stats['total'] }}</span></div><div class="dash-stat"><span class="dash-stat-label">Active</span><span class="dash-stat-value">{{ $stats['active'] }}</span></div><div class="dash-stat"><span class="dash-stat-label">Completed</span><span class="dash-stat-value">{{ $stats['completed'] }}</span></div><div class="dash-stat"><span class="dash-stat-label">Pending</span><span class="dash-stat-value">{{ $stats['pending'] }}</span></div></div>
<div class="crud-card"><div class="crud-header"><h3>Class Roster</h3><a href="{{ route('instructor.enrollments.create') }}" class="btn-add"><i class="fa-solid fa-plus"></i> Enroll Student</a></div><table class="crud-table"><thead><tr><th>Student</th><th>Email</th><th>Status</th><th>Enrolled</th><th>Actions</th></tr></thead><tbody>@forelse($enrollments as $e)<tr><td>{{ $e->student->full_name ?? '—' }}</td><td>{{ $e->student->email ?? '—' }}</td><td>{{ ucfirst($e->status) }}</td><td>{{ $e->enrolled_at?->format('M d, Y') ?? '—' }}</td><td><a href="{{ route('instructor.enrollments.show',$e) }}" class="btn-icon btn-view"><i class="fa-solid fa-eye"></i></a></td></tr>@empty<tr><td colspan="5" style="text-align:center;padding:24px">No students enrolled.</td></tr>@endforelse</tbody></table><div style="padding:14px 24px">{{ $enrollments->links() }}</div></div>
@endsection
