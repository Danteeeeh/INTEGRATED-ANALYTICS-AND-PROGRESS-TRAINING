@extends('layouts.instructor')
@section('title','Calendar — '.$class->code)
@php($activeNav='calendar')
@section('page-title-bar')<div class="page-title-bar"><h2 class="page-title"><i class="fa-solid fa-calendar"></i> Calendar — {{ $class->code }}</h2><div class="page-actions"><a href="{{ route('instructor.classes.calendar.create',$class) }}" class="btn-add"><i class="fa-solid fa-plus"></i> Add Event</a><a href="{{ route('instructor.classes.show',$class) }}" class="btn-modal-cancel">Back</a></div></div>@endsection
@section('content')<div class="crud-card"><div class="crud-header"><h3>Class Events ({{ $events->total() }})</h3></div><table class="crud-table"><thead><tr><th>Event</th><th>Type</th><th>Start</th><th>End</th><th>Visibility</th></tr></thead><tbody>@forelse($events as $event)<tr><td>{{ $event->title }}</td><td>{{ ucfirst($event->event_type ?? 'event') }}</td><td>{{ $event->start_at?->format('M d, Y g:i A') }}</td><td>{{ $event->end_at?->format('g:i A') ?? '—' }}</td><td>{{ ucfirst($event->visibility ?? 'class') }}</td></tr>@empty<tr><td colspan="5" style="text-align:center;padding:24px">No calendar events yet.</td></tr>@endforelse</tbody></table><div style="padding:14px 24px">{{ $events->links() }}</div></div>@endsection
